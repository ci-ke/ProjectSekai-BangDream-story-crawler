[project]
name = "story-crawler"
version = "0.1.0"

requires-python = ">=3.12"
dependencies = [
    "aiofiles>=25.1.0",
    "aiohttp>=3.13.3",
]

[dependency-groups]
dev = [
    "types-aiofiles>=25.1.0.20251011",
]
