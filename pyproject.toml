[project]
name = "story-crawler"
version = "0.1.0"

requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.3",
    "brotli>=1.2.0",
]

[dependency-groups]
dev = [
    "brotli-stubs>=1.2.0",
]
